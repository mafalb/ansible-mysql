---

mysql:
  server:
    package: "{{ mysql_types[mysql_type].package|default('mysql-server') }}"    
    service: "{{ mysql_types[mysql_type].service|default('mysqld') }}"
    additional_packages:
      - MySQL-python
    log_file: /var/log/mysqld.log
