---

mafalb:
  mysql:
    server:
      package: "{{ mafalb_mysql_server_package|default('mariadb-server') }}"    
      daemon: "{{ mafalb_mysql_server_daemon|default('mariadb') }}"
      additional_packages:
        - MySQL-python
