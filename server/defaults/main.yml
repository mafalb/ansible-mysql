---

mysql_type: default
mysql_version: 5.5

mysql:
  type: "{{ mysql_type }}"
  version: "{{ mysql_version }}"
  server:
    manage_package: true
    manage_security: false
    manage_service: true
    manage_config: true
    external_config: false
