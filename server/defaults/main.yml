---

mysql_types:
  mysql_community:
    package: mysql-community-server
    service: mysqld

mysql:
  type: "{{ mysql_type|default('default') }}"
  server:
    manage_package: true
    manage_security: false
    manage_service: true
    manage_config: true
    external_config: false
